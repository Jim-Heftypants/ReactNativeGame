User ID's are generated by hashing username and password
Character ID's are a combination of UserID + character name

User data
Users: {
    UserID: {
        username: String,
        characterList: [name, name, name, name]
    }
}
// in root
UserID: {
    username: String,
    characterList: [name, name, name, name]
}

CharacterID: {
    Name: "Player One",
    RaceName: Races.Skeleton.Name,
    ClassName: Classes.Wizard.Name,
    Attributes: mergeObjects(Races.Skeleton, Classes.Wizard),
    DynamicData: {
        pos: [0, 0],
        AnimEffects: {}, //  { name: data }
        movementSpeed: 1000,
        imageCycleId: 0,
        currentAnimationSet: [],
        Equipment: {

        },
        Items: {

        },
    },
}

CharactersOnline: {
    CharacterID: name,
    CharacterID: name,
    CharacterID: name,
    CharacterID: name,
}