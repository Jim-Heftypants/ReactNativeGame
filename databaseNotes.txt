Unique Identifiers: CharacterName, UserID, conversationID

Users: { // read: Any
    UserID: username (value), // write: UserID
    UserID: username (value), // write: UserID
}

UserID: {
    username (value),
    characterList: {
        one: characterName,
        two: characterName,
        three: characterName,
    },
    conversations: {
        receiverName("," + s): conversationID (value),
        receiverName("," + s): conversationID (value),
    },
    friends: {
        friendID: friendUsername,
        friendID: friendUsername,
    },
    achievements: {
        achievementName: Boolean,
        achievementName: Boolean,
    }
}

conversationID (senderName + "," + receiverName(s) + Timestamp): {
    Timestamp+senderName: {
        Timestamp (value),
        senderName (value),
        message (value),
    },
    Timestamp+senderName: {
        Timestamp (value),
        senderName (value),
        message (value),
    },
}

characterName: {
    RaceName: String,
    ClassName: String,
    Attributes: Object,
    Zone: String,
    Equipment: {},
    Items: {},
    DynamicData: {
        pos: [0, 0],
        AnimEffects: {}, //  { name: data }
        movementSpeed: 1000,
        imageCycleId: 0,
        currentAnimationSet: [],
    },
}

CharactersOnline: {
    characterName,
    characterName,
}